logging:
  level: INFO
  loggers:
    org.molgenis.database: DEBUG
  appenders:
    - type: console

prettyPrintJson: true

databaseConfiguration:
    driver: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/molgenis
    username: molgenis
    password: molgenis